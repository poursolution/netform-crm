'use strict';
const http=require('node:http'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');
const root=path.resolve(__dirname,'..');
async function run(){
 const srv=http.createServer((req,res)=>{const rel=decodeURIComponent(new URL(req.url,'http://127.0.0.1').pathname).replace(/^\/+/, '')||'crm.html',file=path.resolve(root,rel);if(!file.startsWith(root+path.sep)||!fs.existsSync(file)||!fs.statSync(file).isFile()){res.writeHead(404);res.end();return}fs.createReadStream(file).pipe(res)});
 await new Promise(resolve=>srv.listen(0,'127.0.0.1',resolve));
 const browser=await chromium.launch({headless:true,...(process.env.EDGE_PATH?{executablePath:process.env.EDGE_PATH}:{})});
 try{
  const context=await browser.newContext({viewport:{width:1440,height:1000}});await context.route('**/*',route=>new URL(route.request().url()).hostname==='127.0.0.1'?route.continue():route.abort());
  const page=await context.newPage();await page.goto('http://127.0.0.1:'+srv.address().port+'/crm.html',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>!!window.TodayWorkQueue);
  await page.evaluate(()=>{
   const day=n=>{const d=new Date();d.setDate(d.getDate()+n);return [d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-')};
   const recent=new Date(Date.now()-5*36e5).toISOString(),deal=(id,owner,code,due)=>({id,site:id+' 현장',assignee:owner,code,grp:'영업·관리',created:day(-30),lastMeaningfulContactAt:day(-1),nextActionObj:{text:'고객 진행상황 확인',due:day(due),status:'open'}});
   B={inquiryTrash:[],inquiries:[{id:'inq-unassigned',site:'미배정 문의',brand:'POUR솔루션',created_at:recent,status:'접수'},{id:'inq-owned',site:'김성민 문의',brand:'POUR솔루션',created_at:recent,assigned_at:recent,assignee:'김성민',status:'배정완료'}],deals:[deal('pipe-late','김성민','consulting',-3),deal('pipe-today','이필선','sent',0),deal('rel-today','김성민','rapport',0),deal('rel-late','이필선','waiting',-7)],expansion_pool:[{id:'exp-a',source_opportunity_id:'won-a',site:'확장 김성민',owner_name:'김성민',next_contact_at:day(0),expansion_status:'접촉 예정'},{id:'exp-b',source_opportunity_id:'won-b',site:'확장 이필선',owner_name:'이필선',next_contact_at:day(-2),expansion_status:'관계 관리중'},{id:'exp-done',source_opportunity_id:'won-done',site:'전환 완료 확장',owner_name:'김성민',next_contact_at:day(-2),created_opportunity_id:'new-deal'},{id:'exp-held',source_opportunity_id:'won-held',site:'보류 확장',owner_name:'김성민',next_contact_at:day(-2),expansion_status:'보류/휴면'},{id:'exp-inferred',source_opportunity_id:'won-inferred',site:'계산일 확장',owner_name:'김성민',completion_date:day(-40),expansion_status:'신규 대상'}]};
   LOCAL={deals:{},inquiries:{},expansionPool:[]};AUTH_ON=true;ME={name:'송보람',role:'admin'};G.page='today';G.q='무관한 검색';G.rep='없는 담당자';G.expansionYear='2024';G.expansionOwner='없는 담당자';document.getElementById('authGate').classList.remove('on');document.querySelectorAll('.apage').forEach(n=>n.classList.remove('on'));document.getElementById('pg-today').classList.add('on');
   window.__writes=[];pushWrite=(op,p)=>{window.__writes.push({op,p});return 'test'};window.__opened=null;drwDeal=text=>window.__opened={type:'deal',id:JSON.parse(text).id};drwInq=text=>window.__opened={type:'inq',id:JSON.parse(text).id};ExpansionPool.open=id=>window.__opened={type:'expansion',id};todayAssignInquiry=id=>window.__opened={type:'assign',id};dccGoActivity=()=>window.__contact=true;goPage('today');
  });
  assert.equal(await page.locator('.twx-section').count(),1);assert.deepEqual(await page.locator('.twx-section h3').allTextContents(),['관리자 개입 우선순위']);assert.equal(await page.locator('.twx-domains article').count(),4);assert.equal(await page.locator('.twx-row').count(),6,'admin sees exceptions only');assert.equal(await page.locator('.twx-row[data-key="expansion:won-a"]').count(),0,'normal today work is hidden for admin');assert.equal(await page.locator('.twq-counts').count(),1);
  for(const width of [1920,1440,1280,1024,760,390]){await page.setViewportSize({width,height:1000});await page.evaluate(()=>new Promise(requestAnimationFrame));const metrics=await page.evaluate(()=>({overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth,actions:Array.from(document.querySelectorAll('.twx-row')).every(n=>n.querySelectorAll('.twx-actions button').length<=2)}));assert.equal(metrics.overflow,false,'overflow at '+width);assert.equal(metrics.actions,true,'action count at '+width)}
  await page.setViewportSize({width:1440,height:1000});if(process.env.TODAY_WORK_SCREENSHOT)await page.screenshot({path:process.env.TODAY_WORK_SCREENSHOT,fullPage:true});assert.equal(await page.locator('.twx-row').first().getAttribute('data-key'),'inq:inq-unassigned');assert.equal(await page.getByText('전환 완료 확장',{exact:true}).count(),0);assert.equal(await page.getByText('보류 확장',{exact:true}).count(),0);assert.equal(await page.evaluate(()=>TodayWorkQueue.data().rows.filter(x=>x.key==='deal:rel-today').length),1);
  await page.getByRole('combobox',{name:'오늘 업무 담당자'}).selectOption('김성민');assert.equal(await page.getByRole('combobox',{name:'오늘 업무 유형'}).count(),0);await page.locator('.twq-counts [data-filter="overdue"]').click();assert.ok(await page.locator('.twx-row').count()>0);await page.locator('.twq-counts [data-filter="all"]').click();
  await page.locator('.twx-row[data-key="deal:pipe-late"] .twx-actions .quiet').click();assert.deepEqual(await page.evaluate(()=>window.__opened),{type:'deal',id:'pipe-late'});await page.getByRole('combobox',{name:'오늘 업무 담당자'}).selectOption('전체');await page.locator('.twx-row[data-key="deal:rel-late"] .twx-actions button').first().click();assert.deepEqual(await page.evaluate(()=>window.__opened),{type:'deal',id:'rel-late'});assert.equal(await page.evaluate(()=>window.__contact),true);await page.locator('.twx-row[data-key="inq:inq-unassigned"] .twx-actions button').first().click();assert.deepEqual(await page.evaluate(()=>window.__opened),{type:'assign',id:'inq-unassigned'});
  await page.evaluate(()=>{ME={name:'김성민',role:'rep'};paintTodayHome()});assert.equal(await page.getByRole('combobox',{name:'오늘 업무 담당자'}).count(),0);assert.equal(await page.locator('.twx-row').count(),4);assert.equal(await page.locator('.twx-section').count(),2);assert.deepEqual(await page.locator('.twx-section h3').allTextContents(),['지금 먼저 처리하세요','오늘 예정 업무']);assert.equal(await page.locator('.twx-row[data-key="deal:rel-today"]').count(),1);assert.equal(await page.evaluate(()=>TodayWorkQueue.data().rows.every(x=>x.owner==='김성민')),true);await page.evaluate(()=>{window.__opened=null;TodayWorkQueue.open('deal:rel-late')});assert.equal(await page.evaluate(()=>window.__opened),null);
  await page.evaluate(()=>{ME={name:'송보람',role:'admin'};const at=new Date(Date.now()-36e5).toISOString();B.inquiries=Array.from({length:51},(_,i)=>({id:'page-'+i,site:'페이지 현장 '+i,created_at:at,brand:'POUR솔루션',status:'접수'}));B.deals=[];B.expansion_pool=[];paintTodayHome()});assert.equal(await page.locator('.twx-row').count(),20);await page.getByRole('textbox',{name:'오늘 업무 검색'}).fill('페이지 현장 50');await page.getByRole('button',{name:'검색',exact:true}).click();assert.equal(await page.locator('.twx-row').count(),1);assert.equal(await page.evaluate(()=>window.__writes.length),0);
  console.log(JSON.stringify({status:'PASS',admin_sections:['priority','domain_summary'],rep_sections:['priority','today_schedule'],admin_exceptions_only:true,role_scope:true,stale_role_click_blocked:true,viewports:[1920,1440,1280,1024,760,390],external_writes:0}));
 }finally{await browser.close();await new Promise(resolve=>srv.close(resolve))}
}
run().catch(e=>{console.error(e.stack);process.exitCode=1});
