'use strict';
// Real browser + real Staging Auth/read only. It deliberately sends no business write.
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto'),assert=require('node:assert/strict'),{createRequire}=require('node:module');
const {chromium}=createRequire(path.resolve(__dirname,'../../crm-security-lab/package.json'))('playwright');
const REF='rprechiaglyjaydkmxsu',origin='https://'+REF+'.supabase.co',local='http://127.0.0.1:4180';
const credentials=JSON.parse(fs.readFileSync('C:/Users/Administrator/crm-staging-private/auth-synthetic-20260905.json','utf8'));
if(credentials.project_ref!==REF)throw Error('WRONG_PROJECT');
const results=[],network=[],violations=[],errors=[];let browser;
async function check(name,fn){try{await fn();results.push({name,status:'PASS'});console.log('PASS '+name);}catch(e){results.push({name,status:'FAIL',reason:e.name||'ERROR'});errors.push({name,code:e.name||'ERROR'});console.log('FAIL '+name+' '+(e.name||'ERROR'));}}
async function context(){const ctx=await browser.newContext();ctx.setDefaultTimeout(12000);await ctx.route('**/*',async route=>{const req=route.request(),u=new URL(req.url());const rpc=u.pathname.replace('/rest/v1/rpc/',''),ok=u.origin===local||u.origin===origin&&(u.pathname.startsWith('/auth/v1/')||['crm_profile_scoped_v2','crm_read_scoped_v2'].includes(rpc));if(!ok){violations.push({origin:u.origin,path:u.pathname,method:req.method()});return route.abort();}network.push({origin:u.origin,path:u.pathname,method:req.method()});return route.continue();});return ctx;}
async function login(page,file,account){const pc=file==='crm.html';await page.goto(local+'/'+file);await page.locator(pc?'#au-name':'#lg-nm').fill(account.name);await page.locator(pc?'#au-pw':'#lg-pw').fill(account.password);await page.getByRole('button',{name:pc?'로그인':'로그인하기',exact:true}).click();await page.waitForFunction(()=>!!window.Phase1?.profile&&!!window.OperationalUI);await page.waitForResponse(r=>r.url().includes('/rpc/crm_read_scoped_v2')&&r.status()===200);}
async function run(){browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{for(const file of ['crm.html','mobile.html'])for(const account of credentials.accounts)await check(file+' '+account.kind+' operational auth/read/fail-closed',async()=>{const ctx=await context(),page=await ctx.newPage();try{await login(page,file,account);const proof=await page.evaluate(async()=>{const read=await Phase1.read('operational',{limit:100}),before=Phase1.requests.length;let code='';try{pushWrite('activity',{opportunity_id:'00000000-0000-4000-8000-000000000001',type:'TEST'});}catch(e){code=e.code||e.message;}return {profile:{user_id:Phase1.profile.user_id,auth_uid:Phase1.profile.auth_uid},ops:[...OperationalAdapter.operations],coverage:read.coverage,contract:read.data.contract_version,deals:Array.isArray(read.data.deals),inquiries:Array.isArray(read.data.inquiries),pagination:read.data._pagination,unsupported:code,request_delta:Phase1.requests.length-before,blocked:Phase1.blocked.length};});assert.equal(proof.profile.user_id,account.user_id);assert.deepEqual(proof.ops,['opportunity_work_set','inquiry_assign','inquiry_unassign','service_change']);assert.equal(proof.coverage,'scoped_exhausted_per_page');assert.equal(proof.contract,2);assert.equal(proof.deals,true);assert.equal(proof.inquiries,true);assert.equal(proof.pagination.exhausted,true);assert.equal(proof.pagination.scope_completeness,'actor_authorized_rows_only');assert.equal(proof.pagination.snapshot_consistency,'per_page');assert.match(proof.unsupported,/OP_NOT_CONNECTED/);assert.equal(proof.request_delta,0);assert.equal(proof.blocked,0);}finally{await ctx.close();}});}finally{await browser.close();const hash=file=>crypto.createHash('sha256').update(fs.readFileSync(path.resolve(__dirname,'../staging-operational',file))).digest('hex'),report={project_ref:REF,at:new Date().toISOString(),scope:'real browser + real Staging Auth/scoped paged read; no business DML',transport_sha256:hash('transport.js'),pagination_sha256:hash('operational-pagination.js'),results,network,violations,errors,production_requests:network.filter(x=>x.origin.includes('ymfbmpnizxvqsamnczow')).length,n8n_requests:network.filter(x=>/n8n/i.test(x.origin)).length,business_write_requests:network.filter(x=>x.path.includes('/crm_write_command_v2')).length};fs.writeFileSync(path.resolve(__dirname,'../docs/operational-cutover-20260906/operational-ui-browser-read.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify({pass:results.filter(x=>x.status==='PASS').length,fail:results.filter(x=>x.status==='FAIL').length,violations:violations.length,production_requests:report.production_requests,n8n_requests:report.n8n_requests,business_write_requests:report.business_write_requests}));if(results.some(x=>x.status==='FAIL')||violations.length||report.production_requests||report.n8n_requests||report.business_write_requests)process.exitCode=1;}}
run().catch(()=>{console.error('OPERATIONAL_BROWSER_READ_FAILED');process.exitCode=1;});
