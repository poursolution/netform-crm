# 운영 CRM 작업 재개 기준 — 2026-09-18

## 작업 위치와 기준

- 작업 폴더: `C:\Users\User\Documents\ChatGPT\영업운영crm`
- 저장소: https://github.com/poursolution/netform-crm
- 운영 기준 브랜치: `master`
- 작업 브랜치: `codex/crm-continuation`
- 가져온 커밋: `ce3c21d46fd851bae9a14d3c0b10804865644753` (PR #77)
- 운영 주소: https://poursolution.github.io/netform-crm/?view=pc
- 기존 `D:\송보람2\문서\아파트스퀘어 crm` 폴더는 별도 `asq-integrated-system` 저장소다. 운영 CRM 소스와 혼용하지 않는다.

## 완료한 준비

비어 있던 현재 Git 작업 폴더에 origin을 연결하고 운영 master를 가져와 작업 브랜치를 생성했다. 애플리케이션 코드 변경, 원격 push, 배포 및 운영 데이터 변경은 수행하지 않았다.

## 로컬 검증

- `test:contracts`에 지정된 검사: 142 PASS, 실패 및 건너뜀 0.
- 기존 품질 게이트에서 빠져 있던 캠페인 CSV 내보내기, 관계관리 원자 저장 연결, 데이터 정리 저장 실패 보호 검사 10개를 정식 `test:contracts`에 포함했다.
- 대표 대시보드 추가 검사: 6 PASS.
- 대표 대시보드 브라우저 검사: PASS.
- `smoke:pc`에 지정된 12개 명령: 모두 PASS. 오늘 업무, 모바일 완료 실패 보존, 고객자산 페이지/복귀, 글자 크기, 현장 연결 검토, 파이프라인 필터, 견적문의 역할 구분, 담당자 관리, 경남지사 화면 포함.
- 브라우저 검사는 localhost와 합성 데이터를 사용한다. 실제 로그인, 운영 저장, RLS 및 두 세션 동시성 검증을 뜻하지 않는다.
- Node.js 24.19.0. 이 환경에는 npm 명령이 없어 package.json의 명령을 Node로 실행했다.
- 브라우저 검증은 번들 Playwright 1.62.1과 설치된 Edge를 사용했다. 저장소가 고정한 Playwright 1.63.0과 달라 CI 환경과 완전히 동일하지 않다.
- 운영 배포 빌드 재실행은 이번 준비 범위에 포함하지 않았다.

## 최근 운영 상태

- PR #77: 문자 대상 전체 영업이력 CSV 내보내기.
- PR #76: 데이터 정리 서버 기능이 없으면 검토 전용으로 표시.
- PR #75: 동일 Site에 이미 연결된 정상 영업을 연결 후보에서 제외.
- PR #71~72: 오늘 업무 및 파이프라인 개편 전 화면 복원과 캐시 갱신.
- 운영 HTML의 APP_BUILD가 위 기준 커밋과 일치했고 해당 커밋의 Pages 및 Quality Gate 성공을 확인했다.

## 서버 계약 대조 결과

- 관계관리 연락 기록과 다음 연락일 통합 저장은 UI, adapter, 운영 migration 및 rollback까지 소스에 연결돼 있다.
- 저장소 문서에는 운영 DB 미적용이라고 기록돼 있으나, 현재 로그인 세션이나 DB 자격증명이 없어 실제 Production 적용 여부는 확인하지 못했다. 소스 연결만으로 적용 완료라고 판단하지 않는다.
- 데이터 정리의 `crm_cleanup_state`, `crm_cleanup_preview`, `crm_cleanup_apply`는 과거 운영 스냅샷에 없었고, 현재 UI는 계약 부재를 감지하면 검토 전용으로 전환한다.
- 운영 CRM 최신 빌드는 브라우저에서 확인했으나 로그인 전 화면이므로 인증 후 RPC 검증은 수행하지 않았다.

## 후속 확인 순서

1. 데이터 정리의 연결·통합 서버 계약과 운영 적용 여부를 읽기 전용으로 대조한다. 검토 전용 버튼을 단순 활성화하지 않는다.
2. 관계관리 통합 저장 후보가 실제 운영에 연결되었는지 확인한다. 9월 13일 문서의 미완료 기록을 현재 사실로 단정하지 않는다.
3. 구글시트 담당자 동기화의 실제 실행 경로를 확인한다. 기존 문서의 BLOCKED 상태는 재확인 대상이다.
4. 실계정 저장/RLS/동시성 검증은 테스트 계정과 테스트 대상이 확보된 범위에서 수행한다.
5. 고객자산 연결 누락 건수는 9월 16일 감사 수치와 현재 운영 상태를 구분한다.

이 문서는 작업 재개 기준이며 위 후속 항목의 해결 완료를 의미하지 않는다.
