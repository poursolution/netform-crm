/* Admin-only queue for deal/inquiry rows that still lack canonical site_id. */
(function(w){
 'use strict';let section=null,generation=0;const PAGE_SIZE=20;
 const node=(tag,text,cls)=>{const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(cls)n.className=cls;return n;};
 const sourceLabel=row=>{const id=String(row.source_id||'').trim(),short=id?id.slice(-8):'ID 없음';return (row.source_type==='deal'?'영업':'문의')+' · #'+short;};
 const candidateLabel=s=>{const reason=s.match_reason||(s.exact_address?'주소 일치':'이름 일치'),score=Number(s.match_score||0);return reason+(score?' · 근거 '+score:'')+' · '+(s.name||'현장명 미입력')+' · '+(s.address||'주소 미입력');};
 function clear(){generation++;if(section)section.remove();section=null;}
 function mount(root,query=''){
  const transport=w.Phase1,profile=transport?.profile,search=String(query||'').trim().toLocaleLowerCase();if(!root||!profile?.auth_uid||profile.permission_role!=='admin'){clear();return;}
  clear();const actor=profile.auth_uid,ticket=++generation;let items=[],filter='all',page=1;
  section=node('section',undefined,'site-master-section site-link-review');section.dataset.siteRecordReview='true';
  const head=node('header'),titles=node('div'),toolbar=node('nav',undefined,'site-link-review-toolbar'),list=node('div',undefined,'site-link-review-list'),pager=node('nav',undefined,'site-link-review-pager'),retry=node('button','다시 조회','btn');retry.type='button';
  const summary=node('p','Site ID가 없는 영업·문의만 표시합니다. 확정 시 원본 레코드에 Site ID가 저장됩니다.');titles.append(node('h4','Site ID 연결 검토'),summary);head.append(titles,retry);section.append(head,toolbar,list,pager);root.append(section);
  const message=(text,error)=>{list.replaceChildren(node('p',text,'site-nodata'));list.firstChild.setAttribute('role',error?'alert':'status');toolbar.replaceChildren();pager.replaceChildren();};
  const separateAllowed=name=>{const raw=String(name||'').trim(),norm=raw.replace(/[^0-9a-zA-Z가-힣]/g,'').toLocaleLowerCase();return !!raw&&!/^\[[^\]]+\]$/.test(raw)&&!['황윤선전체고객','아파트스퀘어','감리'].includes(norm)&&!/지역미상.*문의[- ]*\d+/.test(raw);};
  const matchesFilter=row=>filter==='all'||filter==='deal'&&row.source_type==='deal'||filter==='inquiry'&&row.source_type==='inquiry'||filter==='candidate'&&(row.site_candidates||[]).length||filter==='separate'&&!(row.site_candidates||[]).length&&separateAllowed(row.name)||filter==='invalid'&&!separateAllowed(row.name);
  async function searchSites(select,link,initial){const query=w.prompt('찾을 Site 이름이나 주소를 2자 이상 입력하세요.',initial||'');if(query===null)return;if(String(query).trim().length<2){w.toast('검색어를 2자 이상 입력해 주세요.');return;}try{const data=await transport.rpc('crm_site_admin_search_v1',{p_query:String(query).trim(),p_limit:20});if(data?.contract_version!==1||!Array.isArray(data.items))throw Error('CONTRACT_MISMATCH');select.replaceChildren(node('option',data.items.length?'검색 결과에서 Site 선택':'검색 결과 없음'));data.items.forEach(s=>{const o=node('option',candidateLabel(s));o.value=s.site_id;select.append(o);});link.disabled=true;if(!data.items.length)w.toast('일치하는 Site가 없습니다. 다른 검색어를 입력하거나 별도 현장으로 확정해 주세요.');}catch(e){w.toast('Site 검색 결과를 불러오지 못했습니다.');}}
  function renderToolbar(){const specs=[['all','전체',items.length],['deal','영업',items.filter(x=>x.source_type==='deal').length],['inquiry','문의',items.filter(x=>x.source_type==='inquiry').length],['candidate','기존 Site 후보',items.filter(x=>(x.site_candidates||[]).length).length],['separate','별도 현장 후보',items.filter(x=>!(x.site_candidates||[]).length&&separateAllowed(x.name)).length],['invalid','현장명 정리',items.filter(x=>!separateAllowed(x.name)).length]];toolbar.replaceChildren(...specs.map(x=>{const b=node('button',x[1]+' '+x[2],filter===x[0]?'on':'');b.type='button';b.onclick=()=>{filter=x[0];page=1;render();};return b;}));}
  function render(){
   renderToolbar();const filtered=items.filter(matchesFilter),pages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE));page=Math.min(page,pages);const rows=filtered.slice((page-1)*PAGE_SIZE,page*PAGE_SIZE);
   if(!rows.length){list.replaceChildren(node('p',search?'검색·필터 조건에 맞는 미연결 레코드가 없습니다.':'Site ID 미연결 영업·문의가 없습니다.','site-nodata'));pager.replaceChildren();return;}
   const frag=document.createDocumentFragment();rows.forEach(row=>{
    const card=node('article',undefined,'site-link-review-row'),info=node('div'),controls=node('div',undefined,'site-link-review-actions'),kind=row.source_type==='deal'?'영업':'문의',e=row.evidence||{},clues=[e.customer_name&&'고객 '+e.customer_name,e.phone&&'연락처 '+e.phone,e.building_address&&'건물주소 '+e.building_address,e.office&&'관리사무소 '+e.office,e.complex&&'단지 '+e.complex,e.work&&'공사 '+e.work,e.inquiry&&'문의 '+e.inquiry].filter(Boolean);info.append(node('strong',row.name||'현장명 미기록'),node('small',sourceLabel(row)+' · '+(row.address||'주소 미입력')+' · '+(row.occurred_at?String(row.occurred_at).slice(0,10):'일자 미기록')));if(clues.length)info.append(node('small','원본 단서 · '+clues.join(' · '),'site-link-evidence'));
    const select=node('select');select.setAttribute('aria-label',(row.name||kind)+' 연결할 Site');select.append(node('option','연결할 Site 선택'));(row.site_candidates||[]).forEach(s=>{const o=node('option',candidateLabel(s));o.value=s.site_id;select.append(o);});
    const canSeparate=separateAllowed(row.name),link=node('button','선택 Site에 연결'),searchButton=node('button','다른 Site 찾기','secondary'),separate=node('button',canSeparate?'별도 현장':'올바른 현장명 입력','secondary');link.type=searchButton.type=separate.type='button';link.disabled=!(row.site_candidates||[]).length;separate.title=canSeparate?'새 canonical Site 생성':'올바른 현장명을 입력해 새 Site를 만듭니다.';
    async function resolve(resolution,site){if(!w.confirm((resolution==='linked'?'선택 Site에 연결':'새 Site를 생성')+'하시겠습니까?'))return;link.disabled=separate.disabled=true;try{const ack=await transport.rpc('crm_site_record_link_review_resolve_v1',{p_source_type:row.source_type,p_source_id:row.source_id,p_resolution:resolution,p_site:site||null});if(ack?.ok!==true||!ack.site_id)throw Error('ACK_MISMATCH');w.toast('Site ID 연결을 확정했습니다.');if(typeof w.refreshOperationalDomains==='function')await w.refreshOperationalDomains([row.source_type==='deal'?'deal_core':'inquiry_core'],'site-link-review');if(typeof w.invalidateSiteMasterData==='function')w.invalidateSiteMasterData();await refresh();}catch(e){link.disabled=!select.value;separate.disabled=false;w.toast('연결 결과를 확인하지 못했습니다. 다시 시도해 주세요.');}}
    async function correctedSeparate(){const name=w.prompt('새 canonical Site의 올바른 현장명을 입력하세요.','');if(name===null)return;if(!separateAllowed(name)){w.toast('집계·지역·임시 문구가 아닌 실제 현장명을 입력해 주세요.');return;}const address=w.prompt('현장 주소를 입력하세요. 모르면 비워둘 수 있습니다.',row.address||'');if(address===null)return;if(!w.confirm(name+'으로 새 Site를 생성하고 연결하시겠습니까?'))return;separate.disabled=true;try{const ack=await transport.rpc('crm_site_record_corrected_separate_v1',{p_source_type:row.source_type,p_source_id:row.source_id,p_site_name:String(name).trim(),p_address:String(address).trim()||null});if(ack?.ok!==true||!ack.site_id)throw Error('ACK_MISMATCH');w.toast('올바른 현장명으로 Site를 만들고 연결했습니다.');if(typeof w.refreshOperationalDomains==='function')await w.refreshOperationalDomains([row.source_type==='deal'?'deal_core':'inquiry_core'],'site-link-review');if(typeof w.invalidateSiteMasterData==='function')w.invalidateSiteMasterData();await refresh();}catch(e){separate.disabled=false;w.toast('새 Site 생성 결과를 확인하지 못했습니다. 다시 시도해 주세요.');}}
    select.onchange=()=>link.disabled=!select.value;link.onclick=()=>resolve('linked',select.value);searchButton.onclick=()=>searchSites(select,link,row.name);separate.onclick=()=>canSeparate?resolve('separate',null):correctedSeparate();controls.append(select,link,searchButton,separate);card.append(info,controls);frag.append(card);
   });
   list.replaceChildren(frag);const prev=node('button','← 이전'),status=node('span',page+' / '+pages+' · '+filtered.length+'건'),next=node('button','다음 →');prev.type=next.type='button';prev.disabled=page===1;next.disabled=page===pages;prev.onclick=()=>{page--;render();};next.onclick=()=>{page++;render();};pager.replaceChildren(prev,status,next);
  }
  async function refresh(){message('Site ID 미연결 영업·문의를 불러오는 중입니다.');try{const data=await transport.rpc('crm_site_record_link_review_list_v1',{p_limit:200});if(ticket!==generation||transport.profile?.auth_uid!==actor)return;if(![1,2,3].includes(data?.contract_version)||!Array.isArray(data.items))throw Error('CONTRACT_MISMATCH');const all=data.items,candidates=all.filter(x=>(x.site_candidates||[]).length).length,separate=all.filter(x=>!(x.site_candidates||[]).length&&separateAllowed(x.name)).length,invalid=all.filter(x=>!separateAllowed(x.name)).length;summary.textContent='미연결 '+all.length+'건 · 기존 Site 후보 '+candidates+'건 · 별도 현장 후보 '+separate+'건 · 현장명 정리 '+invalid+'건';if(typeof w.SiteReviewShortcut==='function')w.SiteReviewShortcut(root,'records','Site ID 미연결',all.length,section);items=all.filter(x=>!search||[x.name,x.address].concat(Object.values(x.evidence||{})).join(' ').toLocaleLowerCase().includes(search));render();}catch(e){if(ticket===generation)message('Site ID 연결 검토 목록을 불러오지 못했습니다.',true);}}
  retry.onclick=()=>{page=1;return refresh();};return refresh();
 }
 w.addEventListener('phase1:identity-cleared',clear);w.addEventListener('phase1:profile',clear);w.PCSiteRecordReview={mount,clear};
})(window);
