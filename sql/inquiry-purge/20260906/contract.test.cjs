'use strict';const {test}=require('node:test'),assert=require('node:assert/strict'),C=require('./adapter-contract.js');
const id='f6090500-0005-4000-8000-000000000001',payload={inquiry_id:id,purged_at:'client',purged_by:'forged'};
test('manual purge strips client actor and time',()=>assert.deepEqual(C.normalize('inquiry_purge',id,0,payload).payload,{intent:'purge'}));
test('other meanings and extra fields fail closed',()=>{assert.throws(()=>C.normalize('inquiry_purge',id,0,{...payload,automatic:true}));assert.throws(()=>C.normalize('inquiry_restore',id,0,payload));});
test('ACK requires irreversible-delete evidence counts',()=>{const cmd={request_id:'f6090600-0096-4000-8000-000000000001',auth_uid:'f6090500-0001-4000-8000-000000000010',user_id:'f6090500-0001-4000-8000-000000000001',...C.normalize('inquiry_purge',id,0,payload)},ack={contract_version:1,ok:true,request_id:'f6090600-0096-4000-8000-000000000001',operation:'inquiry_purge',object_id:id,actor_auth_uid:'f6090500-0001-4000-8000-000000000010',actor_user_id:'f6090500-0001-4000-8000-000000000001',purged:true,purged_at:'2026-09-06T00:00:00Z',purged_by:'TEST ADMIN',removed_scope_count:1,cascaded_assignment_history_count:1,cascaded_next_action_count:0,cascaded_stage_history_count:0,inquiry_audit_event_id:'f6090500-0011-4000-8000-000000000001',replayed:false};assert.equal(C.validateAck(ack,cmd),ack);assert.throws(()=>C.validateAck({...ack,removed_scope_count:-1},cmd));});
