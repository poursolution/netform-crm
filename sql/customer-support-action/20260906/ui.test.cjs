'use strict';
const {test}=require('node:test'),assert=require('node:assert/strict'),build=require('./build.cjs');
const deal='11111111-1111-4111-8111-111111111111';
function payload(){return {client_ref:'support-1788700000000-abc12',target_type:'deal',target_key:'deal:'+deal,opportunity_id:deal,inquiry_id:null,site_name:'표시현장',rep_name:'표시담당',action_key:'call',action_label:'재접촉 실행',reason:'약속일 경과',completion_rule:'접촉 결과 + 다음 Stage 판단',status:'requested',requested_by:'위조 관리자',requested_at:'2026-09-06T00:00:00Z'};}
test('only named repSupportRecord may queue the support operation',()=>{const ui=build.overlay();assert.match(ui,/typeof root\.repSupportRecord==='function'/);assert.match(ui,/op==='customer_support_action'&&!supportIntent/);});
test('adapter preserves UI correlation but strips display authority',()=>{build.build();const a=require('./operational-adapter.candidate.js'),q=a.normalize('customer_support_action',deal,0,payload());assert.deepEqual(q.payload,{client_ref:'support-1788700000000-abc12',target_type:'deal',action_key:'call',action_label:'재접촉 실행',reason:'약속일 경과',completion_rule:'접촉 결과 + 다음 Stage 판단'});assert.throws(()=>a.normalize('customer_support_action',deal,0,{...payload(),status:'completed'}),/CUSTOMER_SUPPORT_ACTION_NOT_CONNECTED/);});
test('read transport hydrates both current support aliases',()=>{const t=build.transport(),ui=build.overlay();assert.match(t,/collect\('customer_support_action'\)/);assert.match(t,/customerSupportActions:customer_support_actions/);assert.match(ui,/customerSupportActions:scoped\.customer_support_actions/);});
