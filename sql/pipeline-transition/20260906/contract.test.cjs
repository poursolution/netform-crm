'use strict';
const {test}=require('node:test'),assert=require('node:assert/strict'),c=require('./adapter-contract.js');
const deal='f6090500-0006-4000-8000-000000000001';
function raw(to='consulting',fields={quote_request:'옥상 방수',required_materials:'도면',quote_due:'2026-09-12'}){const snapshot={transition_date:'2026-09-06',skip_reason:'',memo:'메모',fields,from:'first_contact',to,recorded_at:'client',actor:'client',terminal:false,next:null};return {opportunity_id:deal,from:'first_contact',to,stage_code:'first_contact',at:'client',transition_date:'2026-09-06',note:'견적 요청내용: 옥상 방수',reason:'client',stage_context:snapshot,stage_contexts:{[to]:snapshot},contract_amount:fields.contract_amount,completion_date:fields.completion_date};}
test('structured transition keeps only the parent transaction inputs',()=>{const x=c.normalize(deal,4,raw());assert.deepEqual(x.payload,{from:'first_contact',to:'consulting',transition_date:'2026-09-06',fields:{quote_request:'옥상 방수',required_materials:'도면',quote_due:'2026-09-12'},skip_reason:'',memo:'메모',note:'견적 요청내용: 옥상 방수'});});
test('promote/legacy and terminal transition meanings fail closed',()=>{assert.throws(()=>c.normalize(deal,4,{opportunity_id:deal,from:'first_contact',to:'consulting',note:'문의 승격'}),/TRANSITION_INTENT_NOT_CONNECTED/);assert.throws(()=>c.normalize(deal,4,raw('won',{completion_date:'2026-09-01',completion_checks:['공사 완료','준공검사 완료'],contract_amount:100})),/INVALID_TRANSITION/);});
test('top-level and structured duplicate fields must agree',()=>{const x=raw();x.stage_context.transition_date='2026-09-05';assert.throws(()=>c.normalize(deal,4,x),/INVALID_TRANSITION/);});
