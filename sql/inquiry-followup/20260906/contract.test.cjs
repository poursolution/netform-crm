'use strict';const {test}=require('node:test'),assert=require('node:assert/strict'),C=require('./adapter-contract.js');
const id='f6090500-0005-4000-8000-000000000001',payload={inquiry_id:id,due_at:'2026-09-10',reason:'담당자 연기'};
test('reachable mobile payload is already the canonical scalar-date intent',()=>assert.deepEqual(C.normalize('inquiry_followup',id,0,payload).payload,{due_at:'2026-09-10',reason:'담당자 연기'}));
test('arbitrary reason, datetime and extra Next semantics fail closed',()=>{assert.throws(()=>C.normalize('inquiry_followup',id,0,{...payload,reason:'임의'}));assert.throws(()=>C.normalize('inquiry_followup',id,0,{...payload,due_at:'2026-09-10T00:00:00Z'}));assert.throws(()=>C.normalize('inquiry_followup',id,0,{...payload,action_id:'x'}));});
test('ACK binds the prior and new persisted date to server evidence',()=>{const cmd={request_id:'f6090600-0097-4000-8000-000000000001',auth_uid:'f6090500-0001-4000-8000-000000000010',user_id:'f6090500-0001-4000-8000-000000000001',...C.normalize('inquiry_followup',id,0,payload)},ack={contract_version:1,ok:true,request_id:cmd.request_id,operation:'inquiry_followup',object_id:id,actor_auth_uid:cmd.auth_uid,actor_user_id:cmd.user_id,previous_next_action_date:null,next_action_date:'2026-09-10',status:'배정완료',updated_at:'2026-09-06T00:00:00Z',inquiry_audit_event_id:'f6090500-0011-4000-8000-000000000001',replayed:false};assert.equal(C.validateAck(ack,cmd),ack);assert.throws(()=>C.validateAck({...ack,next_action_date:'2026-09-11'},cmd));});
