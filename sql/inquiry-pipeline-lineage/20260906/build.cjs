'use strict';
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');
const base=require('../../inquiry-response-progress/20260906/build.cjs');
const dir=__dirname,root=path.resolve(dir,'../../..'),sha=x=>crypto.createHash('sha256').update(x).digest('hex');
const read=name=>fs.readFileSync(path.join(dir,name),'utf8').replace(/\r\n?/g,'\n');
const tx=body=>`-- LOCAL REVIEW CANDIDATE ONLY. Staging approval is required.\nBEGIN;\n${body.trim()}\nCOMMIT;\n`;
const twentyThree=`CHECK (operation = ANY (ARRAY['opportunity_work_set'::text, 'inquiry_assign'::text, 'service_change'::text, 'inquiry_unassign'::text, 'favorite_set'::text, 'opportunity_touch'::text, 'next_action'::text, 'activity'::text, 'quote_version'::text, 'next_action_complete'::text, 'stage_check'::text, 'transition'::text, 'close'::text, 'amount'::text, 'waiting_context'::text, 'inquiry_reclassify'::text, 'inquiry_status'::text, 'inquiry_trash'::text, 'inquiry_restore'::text, 'inquiry_purge'::text, 'inquiry_followup'::text, 'opportunity_create'::text, 'lineage_link'::text]))`;
function replaceOne(source,pattern,replacement,label){const next=source.replace(pattern,replacement);if(next===source)throw Error('inquiry pipeline build anchor missing: '+label);return next;}
function applySql(){return tx(read('helper.sql'));}
function rollbackSql(){return tx(read('rollback-fragment.sql'));}
function adapter(){let source=base.adapter();
 source=replaceOne(source,"'inquiry_followup','opportunity_create']","'inquiry_followup','opportunity_create','lineage_link']",'operation list');
 source=replaceOne(source," function normalizeCreate(objectId,payload){",` function promotionStage(status){return /견적.*발송\\s*완료/.test(status)?'sent':'consulting';}\n function promotionReason(status){return /견적.*발송\\s*완료/.test(status)?'견적 발송완료로 파이프라인 인계':/견적.*발송/.test(status)?'견적 준비 단계로 파이프라인 인계':String(status||'').trim()+' 상태로 파이프라인 인계';}\n function normalizePromotion(operation,objectId,payload){const allowed=operation==='opportunity_create'?['intent','inquiry_id','promotion_mode','inquiry_status','owner','from','to','note','amount','name','work_name','brand','site_id','client_ref']:['intent','inquiry_id','promotion_mode','inquiry_status','owner','from','to','note','amount','opportunity_id'];if(Object.keys(payload).some(k=>!allowed.includes(k))||payload.intent!==(operation==='opportunity_create'?'inquiry_promote_create':'inquiry_promote_existing')||operation==='transition'&&payload.opportunity_id!==objectId||!UUID.test(payload.inquiry_id)||!['auto','manual'].includes(payload.promotion_mode)||typeof payload.inquiry_status!=='string'||!/견적.*발송/.test(payload.inquiry_status)||payload.to!==promotionStage(payload.inquiry_status)||payload.note!==promotionReason(payload.inquiry_status)||typeof payload.owner!=='string'||!payload.owner.trim()||payload.owner.length>100||typeof payload.from!=='string')fail('INQUIRY_PROMOTION_INTENT_NOT_CONNECTED');if(operation==='opportunity_create'&&(objectId.length!==36||payload.from!==''||typeof payload.name!=='string'||!payload.name.trim()||typeof payload.brand!=='string'||!payload.brand.trim()||payload.brand==='기술자문'||typeof payload.client_ref!=='string'||!payload.client_ref.trim()))fail('INQUIRY_PROMOTION_INTENT_NOT_CONNECTED');if(payload.site_id!=null&&!UUID.test(payload.site_id)||payload.amount!=null&&(!Number.isSafeInteger(payload.amount)||payload.amount<0)||payload.work_name!=null&&typeof payload.work_name!=='string')fail('INVALID_INQUIRY_PROMOTION');return Object.freeze({...payload,owner:payload.owner.trim(),inquiry_status:payload.inquiry_status.trim(),note:payload.note.trim(),...(payload.name?{name:payload.name.trim()}:{}),...(payload.work_name?{work_name:payload.work_name.trim()}:{}),...(payload.brand?{brand:payload.brand.trim()}:{}),...(payload.client_ref?{client_ref:payload.client_ref.trim()}:{})});}\n function normalizeLineage(objectId,payload){const allowed=['intent','opportunity_id','origin_inquiry_id','site_id','inquiry_site','inquiry_id'];if(Object.keys(payload).some(k=>!allowed.includes(k))||payload.opportunity_id!==objectId){fail('LINEAGE_LINK_INTENT_NOT_CONNECTED');}const inquiry=payload.inquiry_id??payload.origin_inquiry_id;if(!UUID.test(inquiry)||payload.inquiry_id!==undefined&&payload.origin_inquiry_id!==undefined&&payload.inquiry_id!==payload.origin_inquiry_id||payload.intent!==undefined&&payload.intent!=='lineage_link')fail('LINEAGE_LINK_INTENT_NOT_CONNECTED');return {intent:'lineage_link',inquiry_id:inquiry};}\n function normalizeCreate(objectId,payload){if(payload.intent==='inquiry_promote_create')return normalizePromotion('opportunity_create',objectId,payload);`,'promotion normalizers');
 source=replaceOne(source," function normalizeTransition(objectId,payload){const allowed=", " function normalizeTransition(objectId,payload){if(payload.intent==='inquiry_promote_existing')return normalizePromotion('transition',objectId,payload);const allowed=",'transition promotion');
 source=replaceOne(source,"else if(operation==='opportunity_create')normalized=normalizeCreate(objectId,payload);else normalized=normalizeTrash(operation,objectId,payload);", "else if(operation==='opportunity_create')normalized=normalizeCreate(objectId,payload);else if(operation==='lineage_link')normalized=normalizeLineage(objectId,payload);else normalized=normalizeTrash(operation,objectId,payload);",'lineage dispatch');
 source=replaceOne(source,"  if(command.operation==='transition'&&(ack.previous_version", "  if(command.operation==='transition'&&command.payload.intent==='inquiry_promote_existing'&&(ack.intent!==command.payload.intent||ack.inquiry_id!==command.payload.inquiry_id||ack.previous_version!==command.expected_version||ack.version!==command.expected_version+1||ack.to_stage!==command.payload.to||!UUID.test(ack.stage_history_id)||!UUID.test(ack.activity_id)||!UUID.test(ack.audit_event_id)||!UUID.test(ack.inquiry_audit_event_id)||typeof ack.server_at!=='string'))fail('ACK_CONTRACT_MISMATCH');\n  if(command.operation==='transition'&&command.payload.intent!=='inquiry_promote_existing'&&(ack.previous_version",'promotion transition ACK');
 source=replaceOne(source,"  if(command.operation==='opportunity_create'&&(!UUID.test", "  if(command.operation==='opportunity_create'&&command.payload.intent==='inquiry_promote_create'&&(!UUID.test(ack.new_opportunity_id)||ack.intent!==command.payload.intent||ack.inquiry_id!==command.payload.inquiry_id||!UUID.test(ack.site_id)||!UUID.test(ack.owner_id)||!UUID.test(ack.stage_history_id)||!UUID.test(ack.activity_id)||!UUID.test(ack.audit_event_id)||!UUID.test(ack.inquiry_audit_event_id)||ack.version!==1||typeof ack.server_at!=='string'))fail('ACK_CONTRACT_MISMATCH');\n  if(command.operation==='opportunity_create'&&command.payload.intent!=='inquiry_promote_create'&&(!UUID.test",'promotion create ACK');
 source=replaceOne(source,"  return ack;}", "  if(command.operation==='lineage_link'&&(ack.intent!=='lineage_link'||ack.inquiry_id!==command.payload.inquiry_id||ack.opportunity_id!==command.object_id||ack.previous_version!==command.expected_version||!Number.isSafeInteger(ack.version)||ack.version<command.expected_version||typeof ack.changed!=='boolean'||!UUID.test(ack.audit_event_id)||!UUID.test(ack.inquiry_audit_event_id)||typeof ack.server_at!=='string'))fail('ACK_CONTRACT_MISMATCH');\n  return ack;}",'lineage ACK');
 return source;
}
function overlay(){let source=base.overlay();
 source=replaceOne(source,"'inquiry_followup','opportunity_create']);","'inquiry_followup','opportunity_create','lineage_link']);",'overlay operation list');
 source=replaceOne(source,'responseIntent=false,createSurface=null','responseIntent=false,promotionContext=null,createSurface=null','promotion state');
 source=replaceOne(source,"const value=versions.get(id);if(!Number.isSafeInteger(value))throw error('READ_VERSION_REQUIRED');return value;", "let value=versions.get(id);if(!Number.isSafeInteger(value)){const rows=[root.B&&root.B.deals,root.DEALS].filter(Array.isArray);for(const list of rows){const row=list.find(d=>String(d.id||d.opportunity_id)===String(id));if(Number.isSafeInteger(row&&row.version)){value=row.version;versions.set(id,value);break;}}}if(!Number.isSafeInteger(value))throw error('READ_VERSION_REQUIRED');return value;",'read version fallback');
 source=replaceOne(source,"  function applyCreateAck(q){",`  function applyPromotionAck(q){const a=q.ack;versions.set(a.opportunity_id,a.version);for(const rows of [root.B&&root.B.deals,root.DEALS])if(Array.isArray(rows))for(const d of rows){const match=q.operation==='opportunity_create'?String(d.id||d.opportunity_id)===String(q.payload.client_ref):String(d.id||d.opportunity_id)===String(a.opportunity_id);if(match){if(q.operation==='opportunity_create')d.id=d.opportunity_id=d.server_id=a.opportunity_id;d.origin_inquiry_id=d.originInquiryId=a.inquiry_id;d.site_id=a.site_id;d.owner_id=a.owner_id;d.code=d.stage_code=a.to_stage;d.version=a.version;}}for(const rows of inquiryRows())for(const i of rows)if(String(i.id||i.inquiry_id||i.key)===String(a.inquiry_id)){i.originOpportunityId=i.opportunity_id=a.opportunity_id;i.status=a.inquiry_status;i.qualified_at=i.qualifiedAt=a.server_at;}}\n  function applyLineageAck(q){const a=q.ack;versions.set(a.opportunity_id,a.version);for(const rows of [root.B&&root.B.deals,root.DEALS])if(Array.isArray(rows))for(const d of rows)if(String(d.id||d.opportunity_id)===String(a.opportunity_id)){d.origin_inquiry_id=d.originInquiryId=a.inquiry_id;d.version=a.version;}}\n  function applyCreateAck(q){`,'promotion ACK apply');
 source=replaceOne(source,"if(q.operation==='transition')applyTransitionAck(q);", "if(q.operation==='transition'&&q.payload.intent==='inquiry_promote_existing')applyPromotionAck(q);else if(q.operation==='transition')applyTransitionAck(q);",'promotion transition sync');
 source=replaceOne(source,"if(q.operation==='opportunity_create')applyCreateAck(q);", "if(q.operation==='opportunity_create'&&q.payload.intent==='inquiry_promote_create')applyPromotionAck(q);else if(q.operation==='opportunity_create')applyCreateAck(q);if(q.operation==='lineage_link')applyLineageAck(q);",'promotion create sync');
 source=replaceOne(source,"root.pushWrite=function(op,payload){if(op==='inquiry_assign'",`root.pushWrite=function(op,payload){if(promotionContext&&(op==='opportunity_create'||op==='transition')){const c=promotionContext,q=c.q,inquiryId=String(q.id||q.inquiry_id||(root.inqKey&&root.inqKey(q))||'');if(!inquiryId||!c.owner||c.branch)throw error('INQUIRY_PROMOTION_INTENT_NOT_CONNECTED');payload=op==='opportunity_create'?{intent:'inquiry_promote_create',inquiry_id:inquiryId,promotion_mode:c.mode,inquiry_status:String(q.status||''),owner:c.owner,from:'',to:payload.stage_code,note:payload.reason,amount:payload.amount,name:payload.name,work_name:payload.work_name||'',brand:payload.brand,site_id:payload.site_id||undefined,client_ref:payload.client_ref}:{intent:'inquiry_promote_existing',inquiry_id:inquiryId,promotion_mode:c.mode,inquiry_status:String(q.status||''),owner:c.owner,opportunity_id:payload.opportunity_id,from:payload.from,to:payload.to,note:payload.note,amount:payload.amount};}if(op==='inquiry_assign'`,'promotion payload injection');
 source=replaceOne(source,"if(op==='opportunity_create'&&!createSurface)","if(op==='opportunity_create'&&!createSurface&&!promotionContext)",'promotion create gate');
 source=replaceOne(source,"payload={...payload,surface:createSurface,client_ref:String(clientRef),...(createOwnerOverride?{owner:createOwnerOverride}:{})};", "payload=promotionContext?{...payload,client_ref:String(clientRef)}:{...payload,surface:createSurface,client_ref:String(clientRef),...(createOwnerOverride?{owner:createOwnerOverride}:{})};",'promotion create queue');
 source=replaceOne(source,"  const RESPONSE_OUTCOMES=",`  function wrapPromotion(name,mode,select){if(typeof root[name]!=='function')return;const original=root[name];root[name]=function(){const q=select.apply(root,arguments);if(!q)return original.apply(this,arguments);const owner=root.inquirySalesOwner?root.inquirySalesOwner(q):String(q.assignee||'');const branch=(root.itemOwnerTeam&&root.itemOwnerTeam(q)==='gyeongnam')||(root.isTechnicalInquiry&&root.isTechnicalInquiry(q));const previous=promotionContext;promotionContext={q,mode,owner,branch};try{return original.apply(this,arguments);}finally{promotionContext=previous;}};}\n  wrapPromotion('autoPromote','auto',q=>q);wrapPromotion('promoteToPipeline','manual',()=>root.splitSel&&root.splitSel());\n  if(typeof root.linkedDeal==='function'){const legacy=root.linkedDeal;root.linkedDeal=function(q){const id=String(q&&(q.id||q.inquiry_id||(root.inqKey&&root.inqKey(q)))||''),explicit=q&&(q.opportunity_id||q.deal_id||q.originOpportunityId),rows=(root.B&&root.B.deals)||root.DEALS||[],matches=rows.filter(d=>(explicit&&String(d.id||d.opportunity_id)===String(explicit))||String(d.origin_inquiry_id||d.originInquiryId||'')===id);if(matches.length>1)throw error('AMBIGUOUS_INQUIRY_LINEAGE');return matches[0]||legacy.apply(this,arguments);};}\n  const RESPONSE_OUTCOMES=`,'promotion wrappers');
 return source;
}
function build(){const files={'staging-apply-after-inquiry-response.sql':applySql(),'rollback-before-inquiry-response.sql':rollbackSql(),'operational-adapter.candidate.js':adapter(),'operational-overlay.candidate.js':overlay()};for(const [name,value] of Object.entries(files))fs.writeFileSync(path.join(dir,name),value);
 const own=['build.cjs','helper.sql','rollback-fragment.sql','review.md','contract.test.cjs','db.test.cjs','ui.test.cjs','staging-preflight.json',...Object.keys(files)];
 const sources=['sql/inquiry-response-progress/20260906/manifest.json','crm.html','scripts/crm-inquiry-read-compat.cjs','sql/operational-bundle/20260906/after.json'];
 const manifest={project_ref:'rprechiaglyjaydkmxsu',operations:['opportunity_create','transition','lineage_link'],intents:['inquiry_promote_create','inquiry_promote_existing','lineage_link'],status:'DERIVED_SAFE_LOCAL_CANDIDATE_NOT_APPLIED',surface:'pc_only',blocked:['technical_inquiry_transfer','branch_handoff','branch_owner_assign'],lineage_rule:'one inquiry to one Deal; lineage-first read, legacy site fallback only',back_link_rule:'do not write inquiries.deal_id/opportunity_id; preserve existing UI one-way deals.origin_inquiry_id contract',automatic_status_rule:'only /견적.*발송/ status is updated atomically by auto promotion',staging_readonly_preflight:'PASS',staging_ddl_dml_performed:false,production_accessed:false,n8n_accessed:false,files_sha256:Object.fromEntries(own.map(f=>[f,sha(fs.readFileSync(path.join(dir,f)))])),source_sha256:Object.fromEntries(sources.map(f=>[f,sha(fs.readFileSync(path.join(root,f)))]))};fs.writeFileSync(path.join(dir,'manifest.json'),JSON.stringify(manifest,null,2)+'\n');return manifest;}
if(require.main===module)console.log(JSON.stringify(build(),null,2));
module.exports={applySql,rollbackSql,adapter,overlay,build,dir,twentyThree};
