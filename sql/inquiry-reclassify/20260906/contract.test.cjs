'use strict';const {test}=require('node:test'),assert=require('node:assert/strict'),C=require('./adapter-contract.js');
const id='f6090500-0005-4000-8000-000000000001',payload={inquiry_id:id,from_brand:'기술자문',to_brand:'POUR솔루션',review_status:'reclassified',reviewed_at:'2000-01-01',reviewed_by:'forged'};
test('reachable UI payload reduces to server-owned reclassification intent',()=>assert.deepEqual(C.normalize(id,0,payload).payload,{to_brand:'POUR솔루션'}));
test('wrong source/review/target and extra meanings fail closed',()=>{for(const p of [{...payload,from_brand:'POUR솔루션'},{...payload,review_status:'promoted'},{...payload,to_brand:'기술자문'},{...payload,deal_id:'x'}])assert.throws(()=>C.normalize(id,0,p));});
test('ACK requires server actor/time and durable inquiry audit',()=>{const command={...C.normalize(id,0,payload),request_id:'f6090600-0093-4000-8000-000000000001',auth_uid:'f6090500-0001-4000-8000-000000000010',user_id:'f6090500-0001-4000-8000-000000000001'},ack={contract_version:1,ok:true,request_id:command.request_id,operation:'inquiry_reclassify',object_id:id,actor_auth_uid:command.auth_uid,actor_user_id:command.user_id,from_brand:'기술자문',to_brand:'POUR솔루션',review_status:'reclassified',reviewed_at:'2026-09-06T00:00:00Z',reviewed_by:'TEST ADMIN',inquiry_audit_event_id:'f6090500-0011-4000-8000-000000000001',replayed:false};assert.equal(C.validateAck(ack,command),ack);assert.throws(()=>C.validateAck({...ack,inquiry_audit_event_id:null},command));});
